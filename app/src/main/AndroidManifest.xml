<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.expire.reminder">
<uses-permission android:name="android.permission.INTERNET"></uses-permission>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.expire.reminder.login"></activity>
        <activity android:name="com.expire.reminder.setting" />
        <activity android:name="com.expire.reminder.list" />
        <activity android:name="com.expire.reminder.message" />
        <activity android:name="com.expire.reminder.alarm" />
        <activity android:name="com.expire.reminder.notification" />
        <activity android:name="com.expire.reminder.dashboard" />
        <activity android:name="com.expire.reminder.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>